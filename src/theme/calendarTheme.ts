import { Theme } from './theme'

export const calendarTheme = {
  calendarBackground: Theme.colors.background,
  textSectionTitleColor: Theme.colors.text,
  selectedDayBackgroundColor: Theme.colors.primary,
  selectedDayTextColor: '#fff',
  todayTextColor: Theme.colors.primary,
  dayTextColor: Theme.colors.text,
  textDisabledColor: Theme.colors.border,
  arrowColor: Theme.colors.text,
  monthTextColor: Theme.colors.text,
  textMonthFontFamily: Theme.typography.subheader.fontFamily,
  textMonthFontSize: Theme.typography.subheader.fontSize,
  textMonthFontWeight: Theme.typography.subheader.fontWeight,
  textDayFontFamily: Theme.typography.body.fontFamily,
  textDayFontSize: Theme.typography.body.fontSize,
  textDayFontWeight: Theme.typography.body.fontWeight,
  textDayHeaderFontFamily: Theme.typography.body.fontFamily,
  textDayHeaderFontSize: Theme.typography.body.fontSize,
  textDayHeaderFontWeight: Theme.typography.body.fontWeight,
}
